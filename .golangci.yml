version: "2"
linters:
  enable:
    - bodyclose
    - contextcheck
    - depguard
    - errorlint
    - gocritic
    - godot
    - gosec
    - nilerr
    - nilnil
    - noctx
    - nolintlint
    - prealloc
    - predeclared
    - reassign
    - revive
    - staticcheck
    - sloglint
    - unconvert
    - unparam
    - wastedassign
  exclusions:
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling
    paths:
      - third_party$
      - builtin$
      - examples$
  settings:
    depguard:
      rules:
        core:
          files:
            - "**/core/*.go"
          allow:
            - $gostd
            - github.com/dns4acme/dns4acme$
            - github.com/dns4acme/dns4acme/
            - golang.org/x/sync/errgroup
            - github.com/miekg/dns
        kubernetes:
          files:
            - "**/backend/kubernetes/*.go"
            - "**/backend/kubernetes/internal/crd/*.go"
          allow:
            - $gostd
            - github.com/dns4acme/dns4acme$
            - github.com/dns4acme/dns4acme/
            - golang.org/x/sync/errgroup
            - k8s.io/
            - github.com/go-logr/logr
        main:
          files:
            - "!**/core/*.go"
            - "!**/backend/kubernetes/*.go"
            - "!**/backend/kubernetes/internal/crd/*.go"
          allow:
            - $gostd
            - github.com/dns4acme/dns4acme$
            - github.com/dns4acme/dns4acme/
            - golang.org/x/sync/errgroup
            - github.com/miekg/dns
formatters:
  exclusions:
    paths:
      - third_party$
      - builtin$
      - examples$
